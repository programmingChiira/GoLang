// constans in go are similar to variables, only that they use uppercase names and use the const keyword
package main

import (
	"fmt"
)

func main() {
	const NAME = "This is for the constant name"

	fmt.Println(NAME)
}
